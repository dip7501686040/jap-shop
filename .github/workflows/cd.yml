name: Kubernetes Deploy

on:
  workflow_run:
    workflows: ["Docker CI"]
    types:
      - completed
    branches:
      - dev
  workflow_dispatch:
  
jobs:
  deploy:
    name: Deploy to Kubernetes
    runs-on: self-hosted
    if: ${{ github.event_name == 'workflow_dispatch' || github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          sparse-checkout: k8s
          sparse-checkout-cone-mode: false

      - name: Download workflow artifact
        if: ${{ github.event_name == 'workflow_run' }}
        uses: actions/download-artifact@v4
        with:
          name: affected-apps
          path: artifacts
          github-token: ${{ secrets.GITHUB_TOKEN }}
          run-id: ${{ github.event.workflow_run.id }}

      - name: Debug artifacts
        if: ${{ github.event_name == 'workflow_run' }}
        run: |
          echo "Listing current directory:"
          ls -la
          echo "Listing artifacts directory (if exists):"
          ls -la artifacts || echo "Artifacts directory not found"

      - name: Read affected apps
        id: affected
        run: |
          if [ "${{ github.event_name }}" == "workflow_run" ]; then
            FILE="artifacts/affected-apps.json"
            if [ ! -s "$FILE" ]; then
              echo "❌ Artifact file is missing or empty. Nothing to deploy."
              exit 0
            fi

            AFFECTED=$(cat $FILE)
            EMPTY=$(echo "$AFFECTED" | jq 'length == 0')
            if [ "$EMPTY" == "true" ]; then
              echo "✅ No affected apps to deploy."
              exit 0
            fi
          else
            AFFECTED=$(find k8s/* -type d -maxdepth 0 | xargs -n1 basename | jq -R -s -c 'split("\n") | map(select(length > 0))')
          fi

          echo "Affected apps: $AFFECTED"
          echo "affected=$AFFECTED" >> $GITHUB_OUTPUT

      - name: Deploy affected apps to Kubernetes
        env:
          AFFECTED: ${{ steps.affected.outputs.affected }}
        run: |
          echo "🔧 Deploying apps: $AFFECTED"
          for app_name in $(echo "$AFFECTED" | jq -r '.[]'); do
            echo "➡️ Applying Kubernetes config for $app_name"
            kubectl apply -f k8s/$app_name
          done
